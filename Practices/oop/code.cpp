#include<bits/stdc++.h>
using namespace std;

int main (){
    // int x;
    // int digital_sum = 0;
    // cin>>x;

    // while(x>0){
    //     int last_digit = x % 10;
    //     digital_sum += last_digit;
    //     x = x / 10;
    // }

    // string s;
    // string rs = "";
    // cin>>s;

    // for(int n = s.size() - 1; n >= 0; --n){
    //     rs.push_back(s[n]);
    // }


    // string s;

    // cin >> s;
    // bool isSpS = true;

    // for(int n = 0; n <= s.size()/2; n++){
    //     if(s[n] != s[s.size() -n - 1]) {
    //     isSpS = falsejk;
    //     break;
    //     }
    // }

    // if(isSpS)
    // cout << "S is p String";
    // else cout << "S is not p String";

    int n[10];

    n[0] = 10;

    cout << n[0];

    return 0;
}